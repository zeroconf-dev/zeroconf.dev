import { styled } from 'linaria/react';
import { vars } from '../../globals';

// interface Props {}

export const Footer = styled.footer`
	background: ${vars.colors.lightAccent};
	grid-area: footer;
`;
